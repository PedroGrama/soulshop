<section class="m-1">
  <a href="/valepresente/novo" class="btn btn-dark">
    <i class="bi bi-plus"></i>
    Adicionar Vale Presente
  </a>
  <hr />
  <form method="GET">
    <input
      type="text"
      name="codigoValePresente"
      placeholder="Codigo do Vale Presente"
      class="form-control form-control-sm"
      value="{{codigoValePresente}}"
    />
    <button class="btn btn-sm btn-dark mt-1">
      <i class="bi bi-search"></i>
      Buscar
    </button>
  </form>
  <div class="table-responsive">
    <table class="table">
      <thead>
        <th>Codigo</th>
        <th>Data de Validade</th>
        <th>Valor</th>
        <th>Quantidade</th>
        <th>Descrição</th>
      </thead>
      <tbody>
        {{#each valepresente}}
          <tr>
            <td>{{this.codigo}}</td>
            <td>{{this.validade}}</td>
            <td>{{this.descricao}}</td>
            <td>{{this.quantidade}}</td>
             <td>{{this.valor}}</td>
            <td>
              <div class="d-flex">
                <div>
                  <a
                    href="/valepresente/editar/{{this._id}}"
                    class="btn btn-sm btn-warning mx-1"
                  >
                    <i class="bi bi-pencil"></i>
                  </a>
                </div>
                <div>
                  <form action="/valepresente/deletar" method="POST">
                  <input name="id" type="hidden" value="{{this._id}}">
                  <button class="btn btn-sm btn-danger">
                    <i class="bi bi-trash"></i>
                  </button>
                  </form>
                </div>
              </div>
            </td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</section>